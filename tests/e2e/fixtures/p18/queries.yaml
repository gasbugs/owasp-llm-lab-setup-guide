# CUSTOM FILE: publisher-only complete query fixture, never copied into Starter.
# Query languages: LogQL and PromQL; project-authored.
logql: '{service_name="guided-h18-queries"} | request_id = "{request_id}"'
promql: 'sum by (decision) (guided_security_decisions_total{hands_on="H18"})'
trace_lookup: exact_trace_id
