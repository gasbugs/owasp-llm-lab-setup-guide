# CUSTOM FILE: publisher-only incorrect implementation; constants cannot prove change.
logql: '{service_name="guided-h18-queries"} | request_id = "{request_id}"'
promql: 'sum by (decision) (guided_security_decisions_total{hands_on="H18"}) * 0 + 1'
trace_lookup: exact_trace_id
