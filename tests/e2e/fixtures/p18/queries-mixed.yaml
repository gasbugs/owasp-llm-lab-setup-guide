# CUSTOM FILE: publisher-only incorrect implementation; both requests are mixed.
logql: '{service_name="guided-h18-queries"}'
promql: 'sum by (decision) (guided_security_decisions_total{hands_on="H18"})'
trace_lookup: exact_trace_id
